# 更新日志（Changelog）

所有对外可见的变更记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.4.18] - 2026-09-03

### 修复
- 「检测更新」弹窗消失后无任何反应（三处叠加缺陷，全部修复）：
  1. 兜底分支引用了回调作用域里不存在的 `repo` 变量 → 打包版静默 NameError，
     弹窗直接消失且无日志。现 `repo` 随检测结果一并传入，URL 构造恢复正常。
  2. 更新检测此前只查字体清单（manifest），永远看不到 GitHub Release 里的
     安装包。现改为 **GitHub Releases 为主源**（直接取 tag + 便携包资产），
     字体清单仅作 api.github.com 不可达时的版本号兜底。
  3. `updater.py` 模块级漏了 `import socket`，导致该模块全部网络调用
     （检测/下载）必然报"域名解析失败"。已补。
- 版本号口径统一：检测/显示/文件名/下载 URL 一律去 v 前缀，杜绝 "vv0.4.x"。

### 说明
- 本版为自动更新闭环修复验证版：功能与 0.4.17 一致。
- 从 0.4.17 点「检测更新」即可实测完整「检测 → 下载 → 一键安装重启」链路。

## [0.4.17] - 2026-09-03

### 说明
- 自动更新闭环验证版：功能与 0.4.16 一致，专用于「检测更新 → 下载 → 一键安装重启」端到端实测。
- 普通用户从 0.4.16 点「检测更新」即可平滑升入。

### 近期修复回顾（0.4.13–0.4.16 期间补记）
- 检测更新在 DNS 挂起环境下永久卡死：HTTP 请求整体改入 daemon 线程限时执行，
  超时即返回，不再被 getaddrinfo 无限阻塞。
- 「正在检测更新」进度框增加 60 秒看门狗兜底关闭，防重入提示；失败弹窗显示真实错误。
- 日志统一写入 %LOCALAPPDATA%\ChinaSeal\chinatext.log（修复 Path 未导入导致日志静默丢失）。
- 便携更新包固定为 {tag}/ChinaSeal-{tag}-portable.zip 确定性地址，双源（GitHub→AtomGit）自动切换。

## [0.4.3] - 2026-08-31

### 说明
- 更新机制实测版本：功能与 0.4.2 一致，专用于验证软件内自动更新全流程
  （检测 → 下载 → 一键安装 → 自动重启）。普通用户从 0.4.2「检测更新」即可平滑升入。

## [0.4.8] - 2026-08-31

### 说明
- 自动更新全流程验证版：功能与 0.4.7 完全一致。
- 供 v0.4.7 用户实测「检测更新 → 下载 → 一键安装重启」闭环。

## [0.4.7] - 2026-08-31

### 修复
- DNS 解析超时实现缺陷：ThreadPoolExecutor 上下文退出会等待仍挂起的解析线程，
  导致"5 秒超时"形同虚设（域名解析无限挂起时检测更新依然永远卡住）。
  改用 daemon 线程 + join(5s)：超时后主流程立即继续，挂起的解析线程不再阻塞。

[0.4.6] 之前的问题见下文。

## [0.4.6] - 2026-08-31

### 说明
- 更新流程验证版：功能与 0.4.5 完全一致，无任何变化。
- 专用于从 0.4.5「检测更新 → 下载 → 一键安装重启」验证自动更新全流程。

## [0.4.5] - 2026-08-31

### 修复
- DNS 解析无限挂起：域名解析移入限时线程（5 秒硬超时）+ 结果缓存——此前 GitHub 域名解析
  挂起会让检测更新永远等不到结果（即使已线程化）
- 更新包下载地址改为确定性构造（Release 直链），不再依赖 GitHub API 查资产
  （api.github.com 不可达时版本检测仍可走 AtomGit 完成）

### 新增
- About 对话框增加国内镜像（AtomGit）链接

## [0.4.4] - 2026-08-31

### 修复
- 检测更新导致窗口假死（"点了没反应"、任务栏图标变白）：网络检测从 UI 线程移入后台线程，
  UI 全程保持响应；启动时自动检测同样线程化
- 更新安装脚本（helper）路径引号嵌套 bug + 覆盖后完整性检查

## [0.4.10] - 2026-09-02

### 修复
- HTTP 请求全 bounded 化：urlopen（含其内部 getaddrinfo/连接/读取）在 daemon 线程
  限时执行——此前 socket 超时管不到 DNS 解析阶段，域名挂起时检测更新永远等不到结果
- 日志路径统一：更新流程/字体下载/打开日志共用 %LOCALAPPDATA%\ChinaSeal\chinatext.log
  （此前更新日志写到另一路径，导致"日志没有最新信息"）
- About 对话框链接点击改为 linkActivated 显式调起系统浏览器

## [0.4.12] - 2026-09-02

### 说明
- 自动更新全流程验证版：功能与 0.4.11 完全一致。
- About 对话框链接改为 webbrowser.open 显式调起系统浏览器。
- 供 v0.4.11 用户实测「检测更新 → 下载 → 一键安装重启」闭环。

## [0.4.9] - 2026-08-31

### 修复
- 「检测更新」无反馈问题加固：全流程写入诊断日志（chinatext.log）；60 秒看门狗
  （网络卡死时自动复位并提示，不再无限等待）；重复点击给出"检测中"提示
- About 对话框：更新页/镜像页改为**显式按钮**调用系统浏览器打开（QLabel 链接在
  部分环境不响应浏览器调起）

### 变更
- 「打开日志」移到主窗口工具栏（关于前）；About 对话框加"打开 GitHub/AtomGit 页"按钮

## [0.4.1] - 2026-08-31

### 修复
- 检测更新报"未返回版本信息"：改走与字体列表相同的源记忆+兜底链（读 manifest 版本，匿名稳定），失败时显示真实错误
- 默认字体改为覆盖最广的霞鹜文楷（此前默认小篆仅 375 字覆盖，常用字大量缺字）

### 新增
- 内置字体新增思源黑体（简繁全覆盖）
- 缺字预览回显：缺的字改用兜底大字库渲染成琥珀色（导出仍阻止），不再"凭空消失"

## [0.4.0] - 2026-08-31

### 新增
- 软件内自动更新：检测新版本 → 下载（进度条）→ 一键安装并自动重启（helper 脚本等待进程退出后覆盖升级）
- 可选项"每次启动自动检测更新"（启动弹窗内勾选，设置持久化；工具栏检测仍可手动触发）
- 下载源下拉框：自动（记忆优先+兜底）/ 仅 GitHub / 仅 AtomGit（手动切源，指定源失败不兜底）
- 已下载字体置灰标注并禁用重复下载（持久化下载记录 + 文件双查）；全选/全不选互斥按钮
- CHANGELOG.md 更新日志（本文件）

### 修复
- 字体下载"点击无反应"连环问题（PySide 信号类型、slot 异常静默、路径前缀等 4 层）
- GitHub 主源 + AtomGit 兜底链路（源记忆 + 8 秒快速失败）
- Windows exe 文件属性版本信息

## [0.3.3] - 2026-08-31

### 修复
- 字体下载"点了没反应"连环问题（4 层叠加）：
  - PySide 信号类型 `Signal(str)` 传 dict 静默丢信号 → 改 `Signal(object, object)`
  - `_download_selected` 残留旧变量引用导致 worker 不启动
  - `QListWidgetItem.row()` 不存在导致 slot 静默崩溃 → 按索引收集
  - 日志文件父目录未创建导致日志"尚未生成"
- v0.3.2 构建缺 `QComboBox` 导入导致对话框打不开（该问题由回归测试提前捕获）

### 新增
- 手动切源下拉框：自动（记忆优先+兜底）/ 仅 GitHub / 仅 AtomGit（指定源失败不兜底，便于测试）
- 下载源记忆：记住上次成功源，优先直连；GitHub 试探超时压缩到 8 秒
- 已下载字体：持久化下载记录（`downloaded_fonts.json`）+ 文件双查；列表置灰标注"已下载"并禁用勾选；下载自动跳过
- 全选/全不选互斥按钮（跳过已下载项，无可下载项时自动禁用）
- "打开日志"入口移至主窗口工具栏（关于前）；下载日志含完整 traceback

### 变更
- 窗口标题与下载对话框标题显示版本号（随 `__version__` 自动更新）
- exe 增加 Windows 文件属性（版本/公司/版权）
- About 对话框移除公司名（仅窗口标题保留"象形科技"）

## [0.3.0] - 2026-08-30

### 新增
- GitHub 主源 + AtomGit 国内兜底双源链
- AtomGit manifest 分发 + 源记忆

### 修复
- GitHub manifest 字体路径补 `fonts_repo/` 前缀（404 根因）

## [0.2.2] - 2026-08-30

### 首个公开版本（MVP）

- 印面生成：方形/长方形/正圆形章，5-200mm 毫米级尺寸；1-9 字网格 + 单行长条（不限字数）
- 三种读序：现代横排 / 传统竖读（右起）/ 回文环读，读序编号辅助核对
- 边框（毫米可调，白文与底连体）、田字格（仅预览）
- 阴阳刻切换（预览印泥红，导出转黑白）
- 单字微调：拖动 / 旋转 ±180° / 缩放 50-200%，全程撤销/重做
- 水平/垂直镜像
- 导出：矢量 PDF（字形轮廓内嵌+毫米坐标+裁切框）+ PNG（DPI 元数据）+ 毫米校准打印页
- 系统打印对话框直出（真实毫米尺寸）
- 字体管理：系统扫描 + ttf/otf/ttc 导入 + 缺字检测（标红并阻止导出）
- 内置字体：霞鹜文楷、思源宋体、LXGW Seal 小篆（预览版）
- 商用风险字体默认隐藏（方正/汉仪/华文/华康/文鼎/蒙纳等）
- `.chinaseal` 工程文件保存/打开

[未发布]: https://github.com/chentaoxing/chinaseal/compare/v0.3.3...HEAD
[0.3.3]: https://github.com/chentaoxing/chinaseal/compare/v0.2.2...v0.3.3
[0.3.0]: https://github.com/chentaoxing/chinaseal/compare/v0.2.2...v0.3.0
[0.2.2]: https://github.com/chentaoxing/chinaseal/releases/tag/v0.2.2
